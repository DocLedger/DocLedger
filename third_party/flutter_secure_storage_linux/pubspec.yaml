name: flutter_secure_storage_linux
description: Stubbed linux implementation to disable native linkage for desktop demo
version: 1.2.3
homepage: https://pub.dev/packages/flutter_secure_storage_linux

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  # No plugin section to avoid native registration


